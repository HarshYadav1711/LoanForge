# =============================================================================
# LoanForge Web — copy to apps/web/.env.local
# =============================================================================

# Public API base URL (must include the /api suffix; used by all client fetch calls)
NEXT_PUBLIC_API_URL=http://localhost:4000/api
